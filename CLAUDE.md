# ゲンバノワ

建設業の下請案件マッチング＋受発注SaaS。
運営：東北三上機材株式会社（宮城県名取市・足場工事業）

> 仕事が見つかって、取った後の書類まで全部ここで終わる。

## スタック

Next.js (App Router) / TypeScript / Supabase / Vercel / Tailwind
状態管理ライブラリなし。Server Components + Server Actions。ORMなし。

## ディレクトリ

```
src/
  domain/      UIに依存しない。vitestの対象
    shared/ company/ plan/ auth/ job/ conversation/ transaction/ site/
  app/         App Router。actions/ に Server Actions
  components/
    ui/        見た目の部品。ドメインを知らない
    domain/    ドメインを知っている部品
    camera/    黒板合成・LiveCamera
  lib/         supabase / pdf / notify / storage
  styles/      tokens.ts
```

## 色（tokens.ts 以外に書かない）

| 変数 | 値 | 意味 |
|---|---|---|
| sumi | #14171C | 墨・文字・ヘッダ |
| kami | #FFFFFF | 紙・カード |
| yojo | #E9EDEA | 養生・下地 |
| keisen | #D6DBD7 | 罫線 |
| **ki** | #F5C518 | 進行中 |
| **midori** | #00874A | 完了・良好 |
| **aka** | #D22630 | あなたの対応待ち・警告 |
| **usu** | #6B7280 | 終了・停止 |

色の意味はアプリ内に凡例を出している。**実装が揺れると嘘になる。**

## 絶対に守ること

1. **金額の秘匿はサーバー側で落とす。** `field`（現場担当）ロールに
   金額を送らない。CSSで隠すのは不可。日報の人数だけは見せる。
2. **段階開放は取り消さない。** `unlocked_features` は追加のみ。
   未解放画面のURLに直接来たら404にせず解放して表示する。
3. **遅延は確定するまで記録しない。** 期日超過だけでは記録せず、
   `payment_disputes` を通って `recorded` になったものだけ。
   期日内入金は取引ごとに何度でもカウント。
4. **書類は送信後に変更できない。** 訂正は差し戻しか新規発行。
   PDFは生成時に保存し再生成しない。
5. **取引はメッセージからしか始まらない。** 取引タブから単独で
   注文書は作れない。
6. **1枚の請求書に複数の注文書は載せない。** 追加工事は2枚目以降の
   注文書として発行。超過は警告するが拒否しない。
7. **判定は `checkQuota()` と `can()` に集約する。** UIで直接判定しない。
8. **`plan_limits` はテーブルで持つ。** 数値をハードコードしない。

## 用語

- 公共工事 ✕ → **公共事業** ○
- 1次下請 / 2次下請 / 3次下請（商流図は使わない）
- 元請 / 協力会社
- 人工（にんく）／応援（常用）
- 出来高 / 一括 / 是正・手直し / 締め日 / 支払日
- ロール表示：代表者 / 経理・事務 / 現場担当

## 実装順

P0 認証・会社登録・ユーザー招待・段階開放の器
P1 **取引（注文書→請書→日報→請求→入金）** ← ここまでで単体で売れる
P2 会話・メッセージ・取引依頼
P3 案件・応募・空き情報・スカウト
P4 会社ページ・信用スコア・書類提出
P5 工事写真・現場フォルダ
P6 入金確認・異議申立・運営画面
P7 課金（Stripe）・使用量カウンタ
P8 通知（プッシュ／LINE／メール）

## 参照

- `kyoryoku-network-prototype.jsx` — **動く仕様書。迷ったらこれに合わせる**
- `docs/01_実装設計書.md` — DB・RLS・Server Actions
- `docs/04_権限ロール設計.md` — ロールと権限マトリクス
- `docs/02_通知設計書.md` — 全40イベント
- `docs/03_規約・信用情報方針ドラフト.md` — 遅延判定の基準

## 迷ったら

- 仕様書とプロトタイプが食い違ったら**聞く**（プロトタイプが新しいことが多い）
- 業界の慣習で迷ったら**聞く**（依頼者は足場屋の現役）
- 「絶対に守ること」に反する実装が必要になったら**実装前に相談**
